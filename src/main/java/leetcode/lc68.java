package leetcode;

public class lc68 {
    // 每行恰好有 maxWidth 个字符
    // 左右两端对齐的文本
    // 左侧放置的空格数要多于右侧的空格数
//    public List<String> fullJustify(String[] words, int maxWidth) {
//
//        int cnt = 0;
//        int widx = 0;
//        while (widx < words.length && maxWidth + words[widx].length() <= maxWidth){
//            widx += words[widx++].length();
//        }
//
//
//        for (int i = 0; i <words.length ; i++) {
//
//
//        }
//      String line = "";
//    }
}
